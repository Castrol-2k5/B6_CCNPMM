const Product = require("../models/product");

const seedProductsIfNeeded = async () => {
  // Runtime application no longer seeds sample data automatically.
  return;
};

module.exports = {
  seedProductsIfNeeded,
};
